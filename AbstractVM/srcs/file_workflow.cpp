#include "abstractVM.hpp"

void	file_workflow(char *filename)
{
	if (DEBUG)
		std::cout << "Debut File Workflow" << std::endl;

	if (DEBUG)
		std::cout << "File name = " << filename << std::endl;

	if (DEBUG)
		std::cout << "Fin File Workflow" << std::endl;
}
